<h1>user page</h1>


<nav>
  <h1>{{User.username}}</h1>
  <button id="addChar">add toon</button>
  <button id="logoutBtn">logout</button>
</nav>

<main>
  {{#each Toon}}
  <div class="toonCard">
    {{#if Toon.race == Elf && Toon.gender == Female}}
    <img src="../public/assets/images/Elf-female-sketch.png" alt="Elf female">
    {{/if}}
    <h3>{{Toon.name}}</h3>
    <p>{{Toon.backstory}}</p>
  </div>
  {{/each}}
</main>


{{!-- added script for the addChar button to redirect to create toon page --}}

<script>
  console.log("here are users  " + User.username);

  addChar.addEventListener("click", myFunction);

  function myFunction() {
    location.href = "http://localhost:3001/create-toon";
  }

</script>