const router = require('express').Router();
const { Toon } = require('../../models');

// do we even need this file? 
//or will the create toon route just be included
// in the toon-routes js?

module.exports = router;